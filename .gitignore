# Editor / platform noise
.clangd
.DS_Store

# Local shell / agent config
.claude/
.envrc

# Build outputs
/bin/
/obj/
/zip/
/zips/

# Build-time generated static deps
/obj/proto/
/tools/protoc
/lib/libprotobuf-lite.a
/lib/liblibmem.a
/lib/libyaml-cpp.a

# Third-party sources are fetched at build time, not committed.
/third_party/

# Developer notes live in their own repo (docs/developer/); docs/superpowers/ is
# the superpowers working dir. Neither is part of this repo — only docs/LICENSE/ is.
docs/developer/
docs/superpowers/

# Local Deck/debug helpers and one-off probes.
/lua-test/
/tools/cloudbadge_probe*.py

# Removed optional achievement schema helper; achievements are handled internally.
/tools/SLScheevo/

# Codegen output (*.gen.* plus generator stamps)
*.gen
*.gen.*
/src/.patterns-gen.stamp
tools/__pycache__/
